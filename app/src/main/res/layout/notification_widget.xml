<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/notification_widget"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@android:color/transparent"
    android:paddingStart="16dp"
    android:paddingTop="4dp"
    android:paddingEnd="16dp"
    android:paddingBottom="12dp"
    android:visibility="gone">

    <FrameLayout
        android:id="@+id/notification_widget_border"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="112dp"
        android:layout_marginTop="10dp"
        android:background="@drawable/terminal_border"
        android:paddingStart="14dp"
        android:paddingTop="24dp"
        android:paddingEnd="14dp"
        android:paddingBottom="10dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <ScrollView
                android:id="@+id/notification_scroll"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:clipToPadding="false"
                android:fadeScrollbars="false"
                android:fillViewport="false"
                android:scrollbarStyle="outsideOverlay">

                <LinearLayout
                    android:id="@+id/notification_rows"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical" />
            </ScrollView>

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:minHeight="28dp"
                android:layout_marginTop="6dp"
                android:orientation="horizontal">

                <TextView
                    android:id="@+id/notification_widget_prev"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:minHeight="28dp"
                    android:layout_weight="1"
                    android:background="@android:color/transparent"
                    android:fontFamily="monospace"
                    android:gravity="center"
                    android:text="[previous]"
                    android:textColor="#FFFFFF"
                    android:textSize="14sp" />

                <TextView
                    android:id="@+id/notification_widget_next"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:minHeight="28dp"
                    android:layout_marginStart="4dp"
                    android:layout_weight="1"
                    android:background="@android:color/transparent"
                    android:fontFamily="monospace"
                    android:gravity="center"
                    android:text="[next]"
                    android:textColor="#FFFFFF"
                    android:textSize="14sp" />
            </LinearLayout>
        </LinearLayout>
    </FrameLayout>

    <TextView
        android:id="@+id/notification_widget_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignStart="@id/notification_widget_border"
        android:layout_alignTop="@id/notification_widget_border"
        android:layout_marginStart="24dp"
        android:layout_marginTop="-10dp"
        android:background="@drawable/apps_drawer_header_border"
        android:fontFamily="monospace"
        android:gravity="center"
        android:minWidth="170dp"
        android:paddingStart="12dp"
        android:paddingTop="2dp"
        android:paddingEnd="12dp"
        android:paddingBottom="2dp"
        android:text="NOTIFICATIONS"
        android:textColor="#FFFFFF"
        android:textSize="14sp" />

    <TextView
        android:id="@+id/notification_widget_close"
        android:layout_width="48dp"
        android:layout_height="34dp"
        android:layout_alignEnd="@id/notification_widget_border"
        android:layout_alignTop="@id/notification_widget_border"
        android:layout_marginTop="-10dp"
        android:background="@drawable/apps_drawer_header_border"
        android:fontFamily="monospace"
        android:gravity="center"
        android:text="X"
        android:textColor="#FFFFFF"
        android:textSize="14sp" />

</RelativeLayout>
